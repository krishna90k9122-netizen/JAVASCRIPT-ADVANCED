// a+b=result
// console.log(0/0)

let rsult=10+20;
console.log(rsult);

// here the + is operator which is used to add two numbers. In this case, it adds 10 and 20, resulting in 30, which is then logged to the console.